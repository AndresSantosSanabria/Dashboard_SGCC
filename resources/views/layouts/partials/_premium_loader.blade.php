<!-- Modern Premium Loader Overlay Partial -->
<div class="dashboard-loader-overlay">
    <div class="loader-content">
        <div class="premium-spinner"></div>
        <div class="loader-text">{{ $text ?? 'Analizando Información' }}</div>
        <div class="loader-subtext">{{ $subtext ?? 'SGCC Business Intelligence' }}</div>
    </div>
</div>
